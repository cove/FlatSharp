
namespace BenchmarkCore.Modified;

struct Vector4 (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    x:float;
    y:float;
    z:float;
    w:float;
}

struct Vector3Int (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    x:int32;
    y:int32;
    z:int32;
}

struct Vector3  (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    x:float;
    y:float;
    z:float;
}

struct Vector2Int (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    x:int32;
    y:int32;
}

struct Vector2 (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    x:float;
    y:float;
}

struct Color (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    r: ubyte;
    g: ubyte;
    b: ubyte;
    a: ubyte;
}

struct Voxel (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    VoxelType: ubyte;
    SubType : ubyte;
    Hp:ubyte;
    Unused:ubyte;
}

struct MutableUInt32 (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    Value : uint32;
}

struct MutableUShort (fs_writeThrough, xxfs_recyclePoolSize:-1)
{
    Value : ushort;
}

table Mesh (fs_serializer:LazyWriteThrough)
{
    vertices:   [Vector3] (fs_vector:IList);
    normals:    [Vector3] (fs_vector:IList);
    uv:         [Vector2] (fs_vector:IList);
    color:      [Color] (fs_vector:IList);
    triangles:  [MutableUShort] (fs_vector:IList);
    filledLength: MutableUInt32;
}

table VoxelRegion3D (fs_serializer:LazyWriteThrough)
{
    location: Vector3Int;
    iteration: MutableUInt32;
    size: ushort;
    voxels: [Voxel] (fs_vector:IList);
} 